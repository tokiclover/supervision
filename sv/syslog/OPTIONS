#
# $Header:  /etc/sv/syslog/OPTIONS                       Exp $
# $License: 2-clause/new/simplified BSD                  Exp $
#

#
# XXX: Set a syslog choice along with the command to get the service working;
#      which is not necessary, when using an alias like socklog{,-inet,-ucspi,
#      -unix}. Or, else, the service would fail to start.
#
case "${SVC_NAME}" in
	(syslog)
name="syslog-ng"
SVC_CMD="${name}"
	;;
	(socklog-*)
name="socklog"
SVC_CMD="${name}"
	;;
esac

#
# vim:fenc=utf-8:ft=sv:ci:pi:sts=0:sw=4:ts=4:
#
